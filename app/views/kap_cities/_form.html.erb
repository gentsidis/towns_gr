<%= form_for(kap_city) do |f| %>
  <% if kap_city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kap_city.errors.count, "error") %> prohibited this kap_city from being saved:</h2>

      <ul>
      <% kap_city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :prefecture_id %>
    <%= f.text_field :prefecture_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
